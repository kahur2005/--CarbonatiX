# No Supabase project has been provisioned yet. Fill these in once one exists;
# do not put placeholder or fake-looking credentials here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Defaults to http://localhost:8000 (see lib/api.ts) if unset.
NEXT_PUBLIC_API_URL=
